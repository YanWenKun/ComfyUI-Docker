name: Test 'xpu-cn' from Docker Hub

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Publish 'xpu-cn' to Docker Hub"]
    types:
      - completed

jobs:
  test-image-xpu-cn:
    runs-on: ubuntu-latest
    steps:
      - name: Pull image from Docker Hub
        run: docker pull docker.io/yanwk/comfyui-boot:xpu-cn

      - name: Show container environment variables
        run: |
          docker run --rm \
            --name show-env \
            docker.io/yanwk/comfyui-boot:xpu-cn \
            env

      - name: Run container with quick-test
        run: |
          docker run --rm \
            --name comfyui-quick-test \
            -e CLI_ARGS="--quick-test-for-ci --cpu" \
            docker.io/yanwk/comfyui-boot:xpu-cn
